[user]
  name = Teo Ljungberg
  email = teo@teoljungberg.com
  signingKey = B5D3178C
[github]
  user = teoljungberg
[alias]
  a = add --all
  br = branch
  c = commit
  co = checkout
  cp = cherry-pick
  d = diff
  dc = diff --cached
  l = log --oneline
  ls = ls-files

  p = push-fork-or-origin
  r = recent

  ctags = "!sh -c '[ ! -e .git/hooks/ctags ] && git init; .git/hooks/ctags' git-ctags"
  hub = !hub $@
  ignore = "!f() { for p in \"$@\"; do echo \"$p\" >> \"$(git rev-parse --git-dir)/info/exclude\"; done; }; f"
  safe = !mkdir -p $(git rev-parse --git-dir)/safe
[merge]
  tool = vimdiff
[core]
  editor = vim -f
  excludesFile = ~/.gitignore
  pager = less -S
[push]
  default = current
[help]
  autoCorrect = 1
[branch]
  autoSetupRebase = always
[init]
  templateDir = ~/.git_template
[grep]
  lineNumber = true
[fetch]
  prune = true
[rebase]
  autoSquash = true
[pull]
  rebase = true
[color]
  advice = false
  push = false
  transport = false
  ui = false
[commit]
  cleanup = scissors
  verbose = true
[hub]
  protocol = ssh
[include]
  path = ~/.gitconfig.local
