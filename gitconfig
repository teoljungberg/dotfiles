[user]
  name = Teo Ljungberg
  email = teo@teoljungberg.com
  signingkey = B5D3178C
[github]
  user = teoljungberg
[alias]
  changed-files = ~/.bin/git-changed-files
  ctags = ~/.bin/git-ctags
  fixup = ~/.bin/git-fixup
  push-origin-or-fork = ~/.bin/git-push-origin-or-fork
  recent = ~/.bin/git-recent
  remove-merged-branches = ~/.bin/git-remove-merged-branches
  unreleased = ~/.bin/git-unreleased

  a = add --all
  br = branch
  c = commit
  co = checkout
  cp = cherry-pick
  d = diff
  dc = diff --cached
  l = log --oneline
  ls = ls-files
  p = push-origin-or-fork
  r = recent

  hub = !hub $@
  safe = !mkdir -p $(git rev-parse --git-dir)/safe
[merge]
  tool = vimdiff
[core]
  editor = vim -f
  excludesfile = ~/.gitignore
  pager = less -S
[push]
  default = current
[help]
  autocorrect = 1
[branch]
  autosetuprebase = always
[init]
  templatedir = ~/.git_template
[grep]
  linenumber = true
[fetch]
  prune = true
[rebase]
  autosquash = true
[pull]
  rebase = true
[color]
  advice = false
  push = false
  transport = false
  ui = false
[commit]
  cleanup = scissors
  verbose = true
[ghq]
  root = ~/src
[gitsh.color]
  uninitialized = normal
  untracked = normal
  modified = normal
  default = normal
[hub]
  protocol = ssh
[include]
  path = ~/.gitconfig.local
