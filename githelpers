#!/bin/sh

HASH="%C(yellow)%h%C(reset)"
REFS="%C(cyan)%d%C(reset)"
AUTHOR="%C(bold)%an%C(reset)"
SUBJECT="%s"
RELATIVE_TIME="%C(bold)%cr"
SHORT_FORMAT="$HASH$REFS $SUBJECT $RELATIVE_TIME"

short_git_log() {
  git log --graph --pretty="tformat:${SHORT_FORMAT}" $*
}
