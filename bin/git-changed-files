#!/bin/sh

set -e

command -v pick >/dev/null 2>&1 || {
  echo >&2 "pick(1) is not installed."
  exit 78
}

if [ -n "$(git status --porcelain)" ]; then
  git status --porcelain | pick | cut -c4-
else
  exit 1
fi
